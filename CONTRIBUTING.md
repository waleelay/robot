# 贡献与 Git 提交规范

## 1. 提交格式

本项目采用 Conventional Commits（约定式提交）。提交信息格式如下：

```text
<type>(<scope>): <简短中文摘要>

<详细说明>
```

标题与正文之间必须空一行。标题用于概括本次提交，详细背景、实现边界、影响范围和验证结果统一写入 Body；标题不承载完整设计说明。

## 2. 标题约定

`type` 和 `scope` 使用英文，摘要使用简体中文。摘要应以动词开头，准确说明本次提交完成的主要变化，不使用句号结尾。

### 2.1 type

| type | 用途 |
| --- | --- |
| `feat` | 新增功能 |
| `fix` | 修复缺陷或线上问题 |
| `refactor` | 重构，不改变外部行为 |
| `docs` | 文档变更 |
| `test` | 测试新增或调整 |
| `build` | 构建、依赖或部署配置变更 |
| `chore` | 工程维护，不属于上述类型的变更 |
| `perf` | 性能优化 |
| `revert` | 回滚已有提交 |

### 2.2 scope

优先使用受影响的模块或协议范围：

```text
control
media
bff
robot-ui
frontend
gateway
python-client
deploy
protocol
repo
```

如果一次提交确实影响多个模块，可使用最主要的业务范围，并在 Body 的“影响”部分列出全部受影响模块。无法合理归类时可以省略 scope：

```text
docs: 补充本地联调说明
```

## 3. Body 约定

涉及业务逻辑、协议、配置、部署或跨模块修改时，Body 必须使用长描述说明以下内容：

```text
背景：
说明为什么修改，以及原有问题或需求。

变更：
- 说明具体实现内容。
- 说明涉及的接口、事件、配置或生命周期变化。

影响：
- 列出受影响的服务、客户端、前端或部署组件。
- 说明兼容性、边界条件和未改变的职责边界。

验证：
- 列出执行过的测试、构建、联调或线上验证。
- 未执行的验证必须说明原因。
```

简单的纯文档或单行工程维护提交可以使用简短 Body，但标题和变更范围仍必须清晰。

Body 应说明事实，不应把未执行的测试、未部署的代码或未经确认的线上行为写成已完成。

## 4. 示例

```text
fix(video): 修复视频断线后的会话恢复问题

背景：
LiveKit 房间仍存在但媒体轨道已经断开时，前端只根据房间状态判断连接有效，导致视频无法自动恢复。

变更：
- 区分房间存在和有效媒体轨道存在两种状态。
- 无有效轨道时允许重新触发视频会话恢复。
- 固定摄像头继续复用 FIXED_CAMERA、VideoSession 和 LiveKit 链路。

影响：
- control-service：调整视频会话恢复状态判断。
- robot-ui：继续使用现有播放组件和订阅流程。
- 未新增第二套视频播放协议。

验证：
- 已执行相关服务测试。
- 已验证“有房间无轨道”和正常有轨道两种场景。
```

## 5. 破坏性变更

如果提交会破坏现有接口、事件载荷、配置或客户端兼容性，在 type 后增加 `!`，并在 Body 中明确说明迁移要求：

```text
feat(protocol)!: 移除旧版 CAMERA_ZOOM 参数

背景：
旧版参数已不再属于当前协议。

变更：
- 移除服务端旧版兼容解析。
- 同步更新调用方和协议文档。

影响：
- 使用旧参数的客户端必须升级后才能继续调用。
```

## 6. 提交边界

- 一个提交尽量只完成一个可验证的功能闭环。
- 跨服务协议变更必须同时检查生产者、消费者、DTO/模型、WebSocket 或 MQTT 载荷及相关文档。
- 不在提交信息或代码中写入密码、Token、私钥、证书和真实设备凭据。
- 与当前任务无关的已有修改不得覆盖、回退或顺手格式化。
