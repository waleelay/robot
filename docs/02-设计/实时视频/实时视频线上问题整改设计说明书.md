# 实时视频线上问题整改设计说明书

| 文档属性 | 内容 |
| --- | --- |
| 文档状态 | 实施中 |
| 问题基线日期 | 2026-09-10 |
| 适用范围 | 机器人实时视频、大屏监控页、多画面播放、LiveKit、视频会话与设备端 Publisher 协议边界 |
| 适用模块 | `robot-ui`、`bigscreen-bff`、`control-service`、`backend`、`media-common`、LiveKit、EMQX、部署网络 |
| 明确排除 | `python-client` 为模拟 Demo，不作为生产问题证据、不纳入本次代码整改和验收 |
| 线上样本 | 机器人 `sx-songling-001`，服务端 `192.168.124.23` |
| 文档目标 | 固化已发现问题、整改边界、实施顺序和验收标准，作为生产整改与回归依据 |

## 1. 背景与结论

实时视频程序已经上线，但用户长时间观看、多画面观看和断线恢复体验较差。典型表现如下：

- 视频正常播放一段时间后偶发黑屏，页面显示“未连接”。
- 点击 `restart` 后短暂恢复，随后再次无画面。
- 刷新整个页面后通常可以恢复。
- 同一故障期间可能出现重复的视频启动命令、重复 LiveKit Participant 和旧连接回调覆盖新连接状态。
- 浏览器存在 UDP ICE 候选，但部分现场最终选择 LiveKit TCP `7881`，网络丢包或抖动时更容易出现长时间卡顿和 DTLS 超时。

综合线上日志与当前代码确认，本问题不是单一的“机器人推流不稳定”，而是以下问题叠加：

1. 浏览器观看连接与机器人共享 Publisher 的故障域没有隔离。
2. 前端 Room、Track 和 Vuex 状态存在异步竞态，重连成功后仍可能显示未连接。
3. 前端、Media 和 Control 均缺少统一的启动/重启幂等保护。
4. 同源会话创建、Room 生命周期和 Publisher 生命周期没有统一的源粒度所有权。
5. 发布超时、viewer 计数与定时恢复存在数据竞态，在多实例下会被进一步放大。
6. 播放端部分网络未选中 UDP，TCP 链路丢包时恢复能力不足。
7. 线上缺少足以关联浏览器、会话、命令、Room、Participant 和 Track 的观测数据及长稳回归。

整改必须按生产正式方案处理，不能继续依赖用户刷新页面、反复点击重启或服务端盲目重发 start 命令。

## 2. 当前链路与故障边界

```text
robot-ui
  -> Bigscreen BFF
  -> Control Service
  -> Media Service
  -> EMQX
  -> 生产设备端媒体客户端（仓库外）
  -> RTSP
  -> Publisher
  -> LiveKit
  -> 浏览器 WebRTC 播放
```

正常职责边界应为：

- 浏览器只负责加入 Room、订阅 Track、绑定视频元素和恢复自身连接。
- Media 负责 `VideoSession`、viewer、Room、Token、Track 事实和资源引用关系。
- Control 只在确认设备侧 Publisher 需要启动、切换或恢复时下发 MQTT 命令。
- 生产设备端媒体客户端属于外部依赖；本项目只定义并验证 MQTT 命令/状态契约，不修改仓库内 `python-client` Demo。
- LiveKit Participant/Track 是媒体是否真实存在的事实来源，业务状态不能长期与其脱节。

当前实现把上述边界混在一起：浏览器连接失败可以触发机器人 Publisher 重启；单个 session 的释放可以删除共享 Room；多个 session 又可能使用同一个 Room 和 Publisher Identity。

## 3. 线上故障证据

### 3.1 2026-09-10 20:43 至 20:51

- Windows Chrome 播放端的三个摄像头 Participant 多次以 `CLIENT_REQUEST_LEAVE` 关闭并重新加入。
- 部分新旧连接出现 `DUPLICATE_IDENTITY`，说明相同 viewer identity 同时创建了多个 Room 连接。
- LiveKit 随后出现的 `dtls timeout` 多数与连接关闭或替换同时发生，不能简单解释为机器人 Publisher 中断。
- 机器人侧 Participant 和 VP8 Track 在对应时间窗口内仍存在，没有证据表明三个机器人 Publisher 同时停止。
- Control 在同一 session 上曾于 `20:45:25.162` 和 `20:45:25.181` 连续下发两条 start，间隔约 19 ms，且 `commandId` 不同。
- 相同的成对 start 现象在当天多个时间点和多个摄像头上重复出现，属于系统性并发问题。

### 3.2 2026-09-10 21:34 至 21:41

- `21:34:58`，`songling-camera` 和 `camera02` 的 Windows 浏览器 Participant 同时以 `PEER_CONNECTION_DISCONNECTED` 关闭。
- 对应连接出现 `dtls timeout: read/write timeout`，本次属于播放端 WebRTC 传输连接真实中断。
- `camera01` 同期发生明显丢包，LiveKit Remote BWE 将估算带宽从约 `435 kbps` 连续降低到约 `170 kbps`，Stream Allocator 从 `STABLE` 进入 `DEFICIENT`，随后恢复 `STABLE`。
- `21:35:07`，`songling-camera` 和 `camera02` 的新浏览器 Participant 已重新变为 active，但 ICE 最终仍选择 `connectionType=tcp`。
- 服务端在重连后没有新的 Participant close，页面仍显示“未连接”，证明前端显示状态和 LiveKit 实际连接状态已经脱节。

### 3.3 证据结论

| 结论 | 证据等级 |
| --- | --- |
| 浏览器播放连接会真实发生 TCP/DTLS 中断 | 线上日志确认 |
| LiveKit 已重新连接但前端仍显示未连接 | 线上日志与用户现场共同确认 |
| 前端可能并发触发两次 restart | 代码确认，线上成对 start 命令印证 |
| 单个浏览器异常会触发共享机器人 Publisher 重启 | 代码确认 |
| 机器人 Publisher 是本轮所有黑屏的最初原因 | 未确认；已观察到多次黑屏期间 Publisher/Track 仍存在 |
| UDP 端口完全不可达 | 未确认；服务端能产生 UDP 候选，部分客户端曾成功选择 UDP，当前 Windows 样本仍选择 TCP |

## 4. 问题清单

| 问题编号 | 问题摘要 | 级别 | 证据状态 | 主要责任模块 |
| --- | --- | --- | --- | --- |
| RV-P0-001 | 浏览器异常直接触发共享 Publisher 重启 | P0 | 代码确认、线上间接印证 | robot-ui、backend、control-service |
| RV-P0-002 | LiveKit 重连成功后前端状态和画面未恢复 | P0 | 线上日志与现场确认 | robot-ui |
| RV-P0-003 | 前端 restart/connect 缺少统一在途锁 | P0 | 代码确认、线上重复命令印证 | robot-ui |
| RV-P0-004 | Media/Control 重启命令不幂等 | P0 | 代码确认、线上重复命令确认 | backend、control-service |
| RV-P0-005 | 同源启动期间可能创建多个 VideoSession | P0 | 代码确认、线上日志具备疑似现象 | backend、control-service |
| RV-P0-006 | 本地播放失败/人工刷新被实现为 stop/start 共享会话 | P0 | 代码确认 | robot-ui |
| RV-P0-007 | `startCamera` 可将新 session 错绑到旧 Room | P0 | 代码确认 | robot-ui |
| RV-P1-001 | Session 释放与 Room 释放混在一起 | P1 | 代码确认 | backend |
| RV-P1-002 | 机器人业务状态可能与 LiveKit Track 事实脱节 | P1 | 代码确认 | backend、LiveKit |
| RV-P1-003 | UDP 候选存在但部分播放端长期选择 TCP | P1 | 线上日志确认，阻断位置待定位 | 部署网络、LiveKit、客户端网络 |
| RV-P1-004 | 弱网与多路播放缺少明确降载策略 | P1 | 线上拥塞日志确认 | robot-ui、Publisher、部署网络 |
| RV-P1-005 | 状态事件缺少 generation，迟到消息可覆盖新状态 | P1 | 代码确认 | media-common、backend、control-service |
| RV-P1-006 | viewer 心跳会掩盖 Publisher/Track 发布超时 | P1 | 代码确认 | backend |
| RV-P1-007 | viewer 无唯一约束且过期释放状态不完整 | P1 | 代码确认 | backend |
| RV-P1-008 | 定时恢复/释放无多实例单执行者保护 | P1 | 代码确认；生产实例数待核对 | backend、control-service |
| RV-P1-009 | BFF 与跨服务调用的重试边界需固化 | P1 | 代码与架构边界确认 | bigscreen-bff、control-service、backend |
| RV-P2-001 | 全链路可观测性不足 | P2 | 本次排查过程确认 | 全链路 |
| RV-P2-002 | 自动化测试与上线验收不足 | P2 | 测试代码核对确认 | QA、全链路 |
| RV-P2-003 | 前端“未连接”状态过度简化，不能指示真实故障层 | P2 | 代码确认 | robot-ui |

### RV-P0-001 浏览器异常直接触发共享 Publisher 重启

**整改前实现**

`robot-ui` 在 LiveKit `TrackUnsubscribed` 和 `Disconnected` 事件中，只要摄像头仍处于 watching，就调用 `restartCamera`。`restartCamera` 调用 Control `/video-sessions/{sessionId}/restart`，Media 重新生成 Publisher Token 和 start command，Control 再通过 MQTT 请求机器人启动 Publisher。

**问题**

浏览器到 LiveKit 的播放连接和机器人到 LiveKit 的发布连接是两条独立 WebRTC 连接。单个浏览器断网、标签页切换、ICE 失败或 DOM 绑定异常，不能证明共享 Publisher 已失效。

**影响**

- 一个观看端的问题会影响同一 session 下的所有观看者。
- 正常发布中的 Track 被主动重建，扩大故障范围。
- 重启期间所有观看者短暂黑屏。
- 多个浏览器同时异常时可能形成重启风暴。
- 新旧 Participant/Track 事件交错，进一步触发重复恢复。

**整改要求**

1. 删除 `TrackUnsubscribed/Disconnected -> restartCamera` 的直接调用。
2. 浏览器断线首先只恢复 viewer Room，不得下发设备 start。
3. 只有 Media 根据 LiveKit Publisher/Track 事实或机器人状态确认发布端中断时，才允许触发 Publisher 恢复。
4. 人工 restart 保留为明确运维动作，并进行权限、幂等和操作审计。

**验收标准**

- 单个浏览器断网 30 秒后恢复，不产生 MQTT start/restart。
- 同 Room 的其他浏览器连续播放，无黑屏、无 Participant 替换。
- 浏览器恢复后 10 秒内重新出画；失败时展示明确错误，不自动重启共享 Publisher。

### RV-P0-002 前端 Room 重连成功但页面状态未恢复

**当前实现**

前端使用多个异步 Room 事件更新 `camera.room`、`hasVideo`、`remoteVideoTrack` 和 `status`。连接失败、主动断开旧 Room、LiveKit 自动重连、Track 重新订阅和 Vuex 回写可能并发发生。

**问题**

- 缺少明确的 Room generation/epoch，旧 Room 回调可能修改新 Room 状态。
- `Disconnected` 会立即清空 Track 状态，但新 Room 或 SDK 自动重连成功后不一定重新校准 UI。
- 没有以 `RoomEvent.Reconnected + TrackSubscribed/现有 publication` 完整恢复状态。
- Room 已 active 与视频元素已绑定是两个不同阶段，当前使用少量布尔值表达，容易误判。
- Track 已经存在但组件晚挂载时，可能没有重新 attach。

**影响**

- 服务端显示 Participant active，页面仍显示“未连接”。
- restart 后短暂出画，又被旧连接回调清空。
- 刷新页面可以恢复，导致问题难以复现和定位。

**整改要求**

1. 每次创建 Room 分配单调递增的 `roomGeneration`。
2. 所有 Room/Track 回调必须校验 `cameraKey + sessionId + roomGeneration`，过期回调直接忽略。
3. 将播放状态拆分为 `SIGNAL_CONNECTING/CONNECTED`、`TRACK_WAITING/SUBSCRIBED`、`RENDER_ATTACHED/PLAYING` 和 `FAILED`。
4. 监听 `Reconnecting/Reconnected/Disconnected/TrackSubscribed/TrackUnsubscribed`，但只由当前 Room 修改状态。
5. `Reconnected` 后枚举现有 RemoteTrackPublication；已订阅 Track 必须重新绑定当前视频元素。
6. 组件挂载、切换布局和弹窗重开时执行幂等 attach，不依赖 Track 事件再次到达。

**验收标准**

- LiveKit Participant 重新 active 后，UI 状态与 Track/DOM 实际状态一致。
- 旧 Room 延迟触发 `Disconnected` 时，不影响新 Room。
- 页面布局切换、弹窗关闭重开和视频墙重排后无需重新启动 Publisher。
- 不刷新页面连续完成至少 100 次断网/恢复注入。

### RV-P0-003 前端 restart/connect 缺少统一在途锁

**当前实现**

`restartCamera` 检查状态副本上的 `camera.restarting`，随后只修改该副本。`restarting=true` 没有在 HTTP 请求前立即提交为所有事件共享的状态，最终在 5 秒定时器中才回写 Vuex。`connecting` 同样可能因多个 camera 副本而失去互斥效果。

**影响**

- `TrackUnsubscribed` 与 `Disconnected` 连续到达时，两者都可能通过防重判断。
- 同一 session 在毫秒级发出两个 restart HTTP 请求。
- 两次请求生成不同 commandId，MQTT 客户端无法将其识别为 QoS 重投。

**整改要求**

- 使用模块级 `Map<sessionId, Promise>` 或 Vuex 中不可复制的 operation registry 管理 connect/restart/stop。
- 在发 HTTP 前原子登记 in-flight，完成或失败后按同一个 Promise 清理。
- 不使用固定 5 秒定时器作为并发锁。
- stop 的优先级高于 connect/restart；stop 开始后禁止新的恢复动作。
- 为同一 session 的恢复请求设置单调 generation，迟到响应不得覆盖较新状态。

**验收标准**

- 同时注入 `TrackUnsubscribed + Disconnected + session interrupted`，前端最多产生一个恢复操作。
- 20 个并发事件不得产生第二个 restart HTTP 请求。

### RV-P0-004 Media/Control 重启命令不幂等

**当前实现**

Media 每次执行 restart 都创建新的 `cmd_<uuid>`，写入 `REQUESTING_CLIENT` 并返回新的 `VideoStartCommand`。Control 收到命令后直接发布 MQTT，没有针对 session、源或启动阶段的合并保护。

**问题**

- `REQUESTING_CLIENT` 状态仍可再次 restart。
- 同一 session 的并发事务之间没有乐观锁、悲观锁或分布式锁。
- `clientRequestId` 未持久化，也未参与幂等。
- MQTT 设备侧只能过滤相同 commandId，不能过滤业务上相同但 commandId 不同的命令。

**影响**

- 同一 session 出现成对或连续 start。
- 机器人反复停止和启动 Publisher。
- session 当前 commandId 可能被后写事务覆盖，迟到状态无法准确归属。

**整改要求**

1. 定义 `startGeneration` 或 `publisherGeneration`，同一源只能递增发布。
2. `REQUESTING_CLIENT/ROOM_READY` 且命令未超时时，restart 返回当前 in-flight 结果，不创建新 commandId。
3. REST 支持并持久化 `clientRequestId/idempotencyKey`，相同 key 返回相同结果。
4. `VideoSession` 增加 `@Version`，并对源运行态记录使用数据库锁或唯一租约。
5. Control 发布 MQTT 前记录并检查 `sessionId + generation + commandId`，禁止重复发布。

**验收标准**

- 20 个并发 restart 只生成一个 commandId，只发布一条 MQTT start。
- 同一 idempotency key 重试得到完全相同的响应。
- 迟到的旧 generation 状态不能覆盖新 generation。

### RV-P0-005 同源启动期间可能创建多个 VideoSession

**当前实现**

会话复用只查询 `ROOM_READY/STREAMING/IDLE_WAIT`。`INIT/REQUESTING_CLIENT/INTERRUPTED` 不属于可复用状态；创建过程是普通“查询后插入”，没有源粒度并发保护。Room 名称又由源、设备、通道和清晰度确定，不包含 sessionId。

**触发方式**

1. 第一个请求创建 `INIT`。
2. Control 请求 start，状态进入 `REQUESTING_CLIENT`。
3. 第二个 `reuse=true` 请求到达。
4. 第二个请求查不到可复用会话，创建另一个 session。
5. 两个 session 指向同一个 Room。

即使将更多状态加入复用集合，两个请求同时执行“先查后插”仍可能创建重复记录，因此只改状态集合不能彻底解决。

**影响**

- viewer 被分散到不同 session，`viewerCount` 失去源级意义。
- 同 Room 存在多个生命周期控制者。
- 两个 session 可能各自下发 start、stop 和 restart。
- 同一个 Publisher Identity 可能被重复连接。

**整改要求**

- 建立独立的 `VideoSourceRuntime` 或等价源运行态记录，以以下字段构成唯一键：

```text
sourceType + sourceId + deviceId + channel + quality
```

- 所有 session/viewer 引用同一个 source runtime；Room、Publisher generation 和发布状态归 runtime 所有。
- 数据库唯一约束负责最终防重，应用层捕获唯一冲突后读取已有 runtime。
- `INIT/REQUESTING_CLIENT/ROOM_READY/STREAMING/INTERRUPTED/IDLE_WAIT` 均视为源已被占用，差异只体现在恢复动作，不得另建 Room 所有者。

**验收标准**

- 50 个并发 start 最终只有一个 source runtime、一个 Room、一个 Publisher 和一条有效 start。
- 所有调用方获得可绑定到同一 runtime 的 session/viewer 结果。

### RV-P0-006 本地播放失败/人工刷新被实现为 stop/start 共享会话

**当前实现**

- `LeftVideo.resumeVideo()` 在 `HTMLVideoElement.play()` 失败或元素不存在时调用 `startCamera()`。
- `LeftVideo.refreshVideo()` 对已有 session 先执行 `stopCamera()` 再 `startCamera()`；即使 stop 失败也继续 start。
- `play()` 失败可能只是浏览器自动播放策略、DOM 替换、页面不可见或 Track 未 attach，并不代表后端会话或 Publisher 失效。

**影响**

- 本地 UI 问题会进入 Control/Media/MQTT 链路，扩大为共享资源变更。
- stop 失败后继续 start，可造成旧 viewer 未释放、新 viewer/session 又创建。
- 用户连续点击刷新可触发重复启动、Room 误删除和 Participant 替换。

**整改要求**

1. 将“重新播放”定义为前端本地操作：重新枚举 Track、attach 新 DOM、调用 `play()`，必要时重建 viewer Room。
2. 普通刷新不得 stop session，不得调用 Publisher restart。
3. 人工“重启设备推流”作为单独的运维能力，需要二次确认、权限、操作原因和审计日志。
4. stop 失败后不允许无条件 start；必须根据返回的服务端事实决定重试。

**验收标准**

- 模拟 `video.play()` 拒绝、DOM 重建和用户点击刷新，均不产生 start/stop/restart HTTP 和 MQTT 命令。
- 仅重建 viewer Room 即可恢复画面，其他 viewer 无中断。

### RV-P0-007 `startCamera` 可将新 session 错绑到旧 Room

**当前实现**

`startCamera()` 在调用 create API 之前，先基于 store 中旧 `camera.room` 计算 `reuseRoom`。API 返回后会覆盖 `camera.session`；如果事先判定 `reuseRoom=true`，就直接 attach 旧 Room 的 Track，没有比较新旧 `sessionId/roomName`。该方法在有可用 Room 时仍每次调用 create API，并且会无条件清除 `restarting`。

**影响**

- Media 因竞态或状态不可复用而返回新 session 时，store 记录新 session，实际还连着旧 Room。
- 旧 Room 回调、新 session 事件和 heartbeat 同时更新一个 camera，最终表现为服务端 active 而页面“未连接”。
- 新的 start 调用可以提前解除正在执行的 restart 互斥状态。

**整改要求**

- 先按 `cameraKey` 进入统一 start in-flight registry，同一条件的调用复用 Promise。
- 已有当前 session 和可用 Room 时，新增 viewer/consumer 只做本地 attach 与服务端 viewer heartbeat/attach，不重新 create session。
- create 返回后必须核对 `sessionId + roomName`；不一致时将旧 Room 当作过期 generation 有序断开，再连接新 Room。
- 只能由对应 operation 的 `finally` 清理自己的 in-flight 状态，禁止 start 顺手清除 restart。

**验收标准**

- 服务端故意返回不同 session/Room 时，前端最终 session、Room、Token、Track 四者一致。
- 对已播放摄像头连续调用 20 次 start，只增加必要的 attach target，不新建 Room 连接和 session。

### RV-P1-001 Session 释放与 Room 释放混在一起

**当前实现**

`releaseIdleSession(sessionId)` 只检查当前 session：

- 状态是否为 `IDLE_WAIT`；
- 当前 session 的 `viewerCount` 是否为 0；
- 当前 session 自己是否有 `STARTING/ACTIVE` 对讲。

检查通过后直接 unpublish Track、删除 Room、关闭 session，并返回机器人 stop payload。它没有查询相同 roomName 的其他 session、viewer、对讲或录像占用。

**影响**

在重复 session 已经形成时，一个旧 session 的定时释放可以删除另一个活跃 session 正在使用的 Room，导致所有 Participant 和 Track 同时消失。

**整改要求**

- Room 和 Publisher 生命周期归 source runtime 所有，不能由单个 viewer session 直接删除。
- 释放前聚合检查 runtime 下全部 viewer、对讲、录像、启动操作和有效 Track。
- 引用数归零后先进入 `IDLE_WAIT`，到期再次带版本号检查；只有版本未变化才能关闭。
- 删除 Room 与发送 stop 使用同一 release generation，重复执行必须幂等。

**验收标准**

- 构造两个 session 指向同一 Room，释放其中一个不得删除仍有占用的 Room。
- viewer、对讲或录像任意一项存在时均不得停止 Publisher。

### RV-P1-002 机器人视频状态可能与 LiveKit Track 事实脱节

**当前实现**

生产设备端上报 `streaming/track_published` 后，Media 对 `ROBOT_CAMERA` 直接进入 `STREAMING`。后续复用会话时，`hasPublishedTrack()` 对机器人源只检查数据库中 `trackSid/trackName` 非空，不调用 LiveKit Room API。Media 当前也没有消费 LiveKit Webhook。

即使调用 Room API，`resolveVideoTrackSid()` 在 preferred SID 不存在时会返回房间内“第一条视频 Track”，没有校验预期 Publisher Identity、Participant SID 和 generation。`MediaTrackService` 保存的 participant identity 也是根据 session 字段合成，不是 LiveKit 事实。

**影响**

- 业务状态可能是 `STREAMING`，但 Room 中已经没有 Publisher 或视频 Track。
- 前端拿到可复用 session，却长期等不到 Track。
- 自动恢复依赖机器人 MQTT 状态，无法及时识别 LiveKit 单侧中断。

**整改要求**

- 引入 LiveKit Webhook，处理 participant joined/left、track published/unpublished 和 room finished。
- 对关键状态增加 Room API 对账；Webhook 断档时由周期 reconcile 补偿。
- 业务状态保存 LiveKit 返回的真实 Participant Identity/SID、Track SID、最后媒体事实时间和 generation，不再合成事实字段。
- Room API 对账必须按当前 runtime 的预期 publisher identity 精确查找，禁止使用房间中任意第一条视频 Track。
- `STREAMING` 必须同时满足当前 generation Publisher active 且视频 Track published。

**MS-04 已实现边界（2026-09-11）**

- LiveKit 对 `participant_joined/left/connection_aborted`、`track_published/unpublished`、
  `room_finished` 的签名 Webhook 触发对账；Media 校验 JWT issuer、签名和原始正文 SHA-256。
- Webhook 不直接修改业务状态，而是按 Room 定位唯一 SourceRuntime，再调用 Room API 精确查询
  预期 Publisher Identity 下的视频 Track；5 秒周期任务补偿 Webhook 丢失和乱序。
- SourceRuntime 保存实际 Publisher Identity、Participant SID、Track SID/名称和最后媒体事实变化时间；
  稳定事实不重复写库或广播事件。
- 设备端 `streaming/track_published/interrupted/stopped` 不再单独决定媒体事实。实际 Track 出现后进入
  `STREAMING`；已推流会话的 Track 消失后，有观看、对讲或录像占用则进入 `INTERRUPTED`，否则进入
  `IDLE_WAIT`，复用既有 Control 恢复调度。
- 本项不引入 generation，也不宣称多 Media/Control 实例下仅恢复一次；generation 归 CT-01，
  分布式调度 claim 归 MS-07/CS-03。完整验收需在这两项完成后联合关闭。

**验收标准**

- 强制踢出机器人 Participant 后，Media 在 10 秒内进入 `INTERRUPTED` 并启动一次受控恢复。
- MQTT 报 streaming 但 LiveKit 无 Track 时不得向前端宣称可播放。

### RV-P1-003 UDP 候选存在但部分播放端长期选择 TCP

**线上现象**

LiveKit 对外产生 `211.137.109.150:50000-50100/UDP` 候选，但 Windows 现场样本最终选择 `TCP 7881`。同一现场也观察到其他客户端成功使用 UDP，因此不能简单判断为服务端 UDP 全局关闭。

**可能范围**

- 客户端出口网络或安全软件阻止 UDP。
- 公网 NAT/端口映射只完成入站配置，返回路径、会话保持或上游 ACL 不完整。
- LiveKit `external_ip`、Docker UDP 映射与公网地址不一致。
- 多层 NAT、运营商网络或企业代理导致 UDP ICE 检测失败。

**影响**

- WebRTC 媒体复用 TCP，丢包时触发队头阻塞。
- 三路视频同时播放时更容易出现带宽拥塞、卡顿和 DTLS 超时。
- TCP 自动降级本身可用，但不能作为长时间多路播放的首选路径。

**整改要求**

1. 从真实大屏网络对完整 UDP 端口段执行双向验证，不能用普通 `nc -z` 的成功作为 UDP 可用结论。
2. 在服务端同步抓包，确认客户端探测包到达且响应能够返回。
3. 使用 Chrome `chrome://webrtc-internals` 保存选中 candidate pair、RTT、丢包、NACK、PLI 和可用带宽。
4. 核对云防火墙、宿主机防火墙、NAT、Docker 映射和 LiveKit advertised IP。
5. 保留 TCP `7881` 作为降级通道，并对 TCP 播放单独执行弱网恢复测试。

**验收标准**

- 允许 UDP 的大屏网络中，三个 Room 均选中 `udp` candidate pair。
- UDP 被明确阻断时可降级 TCP，并在短时网络抖动后恢复，不出现永久“未连接”。
- 监控能够按 Room/Participant 展示最终协议为 UDP 或 TCP。

### RV-P1-004 弱网与多路播放缺少明确的降载策略

**线上现象**

`camera01` 曾因 LOSS 将可用带宽估算降至约 `170 kbps`，Stream Allocator 进入 `DEFICIENT`。当前页面同时订阅三路视频，视频源默认可能持续发布高于现场可用带宽的码率。

**整改要求**

- 视频墙默认使用 sub 码流，单路放大后再切 main。
- 明确机器人 RTSP 主/子码流分辨率、帧率和目标码率上限。
- 评估并启用适合当前 Publisher 的 Simulcast/SVC；不支持时按页面可见性暂停不需要的订阅。
- 收集 inbound-rtp 丢包、jitter、framesDecoded、freezeCount 和 keyframe 指标。
- 弱网进入阈值后先降清晰度或暂停非主画面，不自动重启 Publisher。

**验收标准**

- 三路 sub 码流总码率不超过现场网络容量预算的 70%。
- 注入 5% 丢包和 300 ms RTT 时，主画面可恢复且不触发共享 Publisher restart。

### RV-P1-005 状态事件缺少 generation，迟到消息可覆盖新状态

**问题**

启动、重启和切换都会产生新的发布尝试，但客户端状态主要按 sessionId 更新。旧 Publisher 或旧命令的迟到 `publishing/streaming/failed/stopped` 可能覆盖新发布尝试的状态。

**整改要求**

- start、stop、restart、switch 和 status 统一携带 `runtimeId + generation + commandId`。当前 `VideoStatusMessage` 连 `commandId` 都没有，该 DTO、MQTT 订阅转发和 Media 状态接口必须同步升级。
- Media 只接受当前 generation 的状态；旧 generation 仅记录审计日志。
- stop 必须指定要停止的 generation，不能让旧 stop 关闭新 Publisher。

**验收标准**

- 人工乱序投递旧 streaming/failed/stopped，不改变当前发布状态。

### RV-P1-006 viewer 心跳会掩盖 Publisher/Track 发布超时

**当前实现**

`VideoSessionTimeoutScheduler` 通过 `status + updatedAt < threshold` 查找 `REQUESTING_CLIENT/ROOM_READY` 超时会话；而前端每 5 秒发送 viewer heartbeat，`heartbeat()` 每次都更新同一个 session 的 `updatedAt`。

**影响**

- 只要页面仍在心跳，永远没有 Track 的启动就可能永远达不到超时阈值。
- 前端持续显示“未连接/正在连接”，Media 不进入 `TIMEOUT/FAILED`，也不会给出可恢复的明确错误。

**整改要求**

- 发布超时只使用 `commandRequestedAt` 或独立 `publishDeadlineAt`，不得使用通用 `updatedAt`。
- 心跳只更新 viewer 的 `lastHeartbeatAt`；如需保存 session 活跃时间，使用独立 `lastViewerActivityAt`。
- 超时更新必须带 `generation` 条件，避免旧扫描结果将已成功的新 generation 置为失败。

**验收标准**

- viewer 保持 5 秒心跳但 Publisher 不发布 Track，会话在配置超时时间加一个扫描周期内进入明确失败状态。
- Track 在超时边界成功发布时，只有一个带版本条件的终态生效。

### RV-P1-007 viewer 无唯一约束且过期释放状态不完整

**当前实现**

- `addViewer()` 采用“先查再插入”，但 `media_session_viewer` 没有 `sessionId + participantIdentity + active` 唯一约束。
- `removeViewer()` 只关闭查到的第一条活跃记录；竞态形成的重复 viewer 可继续计数。
- 心跳过期的 `closeViewer()` 只在 session 状态为 `STREAMING` 时转入 `IDLE_WAIT`。`REQUESTING_CLIENT/ROOM_READY/INTERRUPTED/FAILED/TIMEOUT` 在 viewer 归零后可能长期滞留，而 idle release 又只扫描 `IDLE_WAIT`。
- Media 服务每次启动都会关闭数据库中全部 active viewer，却没有与浏览器 Room 连接建立恢复代际。

**整改要求**

1. 引入稳定 `viewerLeaseId`，数据库对 `runtimeId + viewerLeaseId` 建唯一约束；heartbeat 使用 upsert/条件更新。
2. stop 和过期必须关闭该 lease 的全部异常重复记录，并从表内实时聚合引用数，不信任可漂移的 `viewerCount` 缓存值。
3. 任何非终态 session/runtime 在所有引用归零后都必须进入统一的 idle/release 状态机。
4. 服务重启不应立即将所有 viewer 判死；使用 lease TTL 自然收敛，或为重启建立短暂 grace period，允许活跃浏览器续租。

**验收标准**

- 50 个并发心跳只有一个 active viewer lease。
- 所有中间/失败状态在最后 viewer 过期后均能进入 idle 并安全释放。
- 重启 Media 后仍在心跳的页面不会导致 Publisher 停止或 Room 被删除。

### RV-P1-008 定时恢复/释放无多实例单执行者保护

**当前实现**

Control 每 5 秒查询 interrupted/idle 候选并直接执行 restart/release；Media 也每 5 秒执行超时和 viewer 清理。代码中没有 ShedLock、分布式租约、数据库 `SKIP LOCKED` 领取或 generation CAS。Java `synchronized` 仅限制单 JVM。

**影响**

- 生产一旦水平扩容，多个 Control 实例可对同一 session 同时下发 restart/stop。
- 多个 Media 实例可对同一 viewer/session 同时过期或标记超时。
- 即使当前 compose 只启动一个实例，代码也不具备无损扩容边界。

**整改要求**

- 对每个 runtime/generation 采用数据库原子 claim：只有从 `INTERRUPTED -> RECOVERING` 或 `IDLE_WAIT -> RELEASING` 成功的执行者才能产生外部副作用。
- 定时任务全局锁可作为短期止损，但最终仍要求单 runtime 的状态 CAS 和副作用幂等。
- 所有 claim 必须有超时和接管机制，避免执行者崩溃后永久卡在 `RECOVERING/RELEASING`。

**验收标准**

- 同时启动 2 个 Control 和 2 个 Media 实例，单次中断只产生一条有效 start，单次 idle 只产生一条 stop 和一次 Room delete。
- claim 执行者在副作用前崩溃，租约超时后可由其他实例幂等接管。

### RV-P1-009 BFF 与跨服务调用的重试边界需固化

**当前边界**

`bigscreen-bff` 当前主要是鉴权后的 REST/WebSocket 透传，不承载媒体流，也不应持有 VideoSession/Room/Publisher 状态。此边界是正确的，但 start/restart/stop 在端到端幂等落地前属于非幂等变更，任意一层的超时自动重试都可创建重复副作用。

**整改要求**

- BFF 只负责传递身份、`traceId/clientRequestId` 和原始返回状态，不实现视频恢复决策。
- 在服务端幂等之前，BFF、Nginx、HTTP 客户端不得对 start/restart/stop 自动重试；仅 GET/明确幂等查询可重试。
- 幂等完成后，重试必须复用同一 `clientRequestId`，不得每次生成新 key。

**验收标准**

- 模拟 Control 已执行但 BFF 读取超时，重试后仍只存在一个 session/runtime 和一条设备命令。
- BFF 不出现 Room 管理、Track 判定或 MQTT 补发逻辑。

### RV-P2-001 可观测性不足，线上只能跨容器人工拼日志

**当前缺口**

- 前端未统一记录 Room generation、连接状态变化、Track SID 和选中 ICE candidate pair。
- 服务端日志缺少贯穿全链路的统一关联字段。
- 页面只显示“未连接”，没有区分信令失败、ICE 失败、无 Publisher、无 Track、未绑定 DOM 或解码冻结。
- 缺少重复 start、Duplicate Identity、DTLS timeout、Room 误删除和重连耗时告警。

**整改要求**

统一以下关联字段：

```text
traceId, clientRequestId, userId, clientId,
sourceRuntimeKey, sessionId, roomName, roomGeneration,
publisherGeneration, commandId,
publisherParticipantSid, viewerParticipantSid, trackSid,
connectionType, selectedCandidatePair, disconnectReason
```

前端提供可下载的诊断快照，至少包含最近 200 条 Room/Track 状态事件和当前 WebRTC stats 摘要；不得包含 Token、RTSP 凭据或敏感 SDP。

**告警建议**

| 指标 | 告警条件 |
| --- | --- |
| 同 session start 次数 | 60 秒内大于 1 |
| 同 source active runtime | 大于 1 |
| `DUPLICATE_IDENTITY` | 任意生产发生即告警 |
| viewer reconnect latency | P95 大于 10 秒 |
| `PEER_CONNECTION_DISCONNECTED` | 5 分钟内同 client 大于 3 |
| TCP 播放占比 | 超过现场约定阈值 |
| Room 删除时活跃引用数 | 必须恒等于 0 |

### RV-P2-002 自动化测试与上线验收不足

**当前缺口**

现有测试没有覆盖本次核心竞态：

- 同源并发创建 session。
- `TrackUnsubscribed + Disconnected` 同时发生。
- 旧 Room 回调覆盖新 Room。
- restart HTTP 并发幂等。
- 两个 session 共享 Room 时的释放保护。
- 机器人收到不同 commandId 的重复 start。
- UDP/TCP 两种传输的长时间三路播放。

整改完成后必须补充第 8 节测试矩阵，未执行或失败不得以“刷新可恢复”作为上线接受条件。

### RV-P2-003 前端“未连接”状态过度简化

`VideoBox` 在没有 `hasVideo` 时主要显示“未连接”，又将 `STREAMING/IDLE_WAIT` 等多个业务状态归入“正在连接”。页面无法告诉用户究竟是信令连接、ICE、Publisher、Track、订阅、DOM attach、自动播放还是解码冻结失败。

**整改要求**

- 展示分层状态和可操作错误码，不向普通用户暴露 Token、SDP 和内部网络信息。
- “重试播放”只修复 viewer；只有运维诊断显示 Publisher/Track 真实缺失时，有权用户才能选择“重启推流”。
- 前端错误码、Media 状态和 LiveKit 断开原因建立映射，并纳入诊断快照。

**验收标准**

- 任意黑屏可在一次诊断快照中定位到上述七个阶段之一，不再只有“未连接”。

## 5. 目标状态与所有权模型

### 5.1 三类对象必须分离

| 对象 | 粒度 | 所有者 | 生命周期 |
| --- | --- | --- | --- |
| ViewerSession | `userId + clientId + sourceRuntime` | Media | 单浏览器观看和心跳 |
| SourceRuntime | `sourceType + sourceId + deviceId + channel + quality` | Media | Room 与 Publisher 的唯一运行态 |
| BrowserRoomConnection | `clientId + roomGeneration` | robot-ui | 单浏览器 LiveKit 连接与 Track 绑定 |

单个 ViewerSession 的离开只能减少引用；不能直接关闭 SourceRuntime。单个 BrowserRoomConnection 的失败只能恢复浏览器连接；不能直接重启 Publisher。

### 5.2 目标恢复决策

```text
浏览器 Disconnected
  -> 前端重连当前 Room
  -> 重签 viewer Token（仅 Token 失效时）
  -> 重新订阅并 attach Track
  -> 不发送 MQTT start

Media 确认 Publisher/Track 消失
  -> SourceRuntime 进入 INTERRUPTED
  -> 生成唯一的新 publisherGeneration
  -> Control 发送一条 MQTT start
  -> 机器人受控替换 Publisher
```

### 5.3 Room 释放决策

```text
viewerCount == 0
and intercomCount == 0
and recordingCount == 0
and startOperationCount == 0
and publisherGeneration 未变化
and idle deadline 已到
  -> stop 当前 Publisher generation
  -> 等待或确认 Participant/Track 离开
  -> delete Room
  -> close SourceRuntime
```

### 5.4 模块整改边界

| 模块/系统 | 必须负责 | 明确不负责 |
| --- | --- | --- |
| `robot-ui` | viewer Room 连接、Track 订阅/attach、播放状态、本地重连、WebRTC stats | 不以浏览器事件判定共享 Publisher 失效，不自动下发设备 restart |
| `bigscreen-bff` | 鉴权代理、身份/关联 ID 透传、WebSocket 事件转发 | 不承载媒体流，不复制会话状态机，不对非幂等变更盲目重试 |
| `control-service` | 权限、业务入口、幂等命令发布、设备状态转发 | 不创建第二份 Room/runtime 真相，不根据 viewer 断线自主恢复 Publisher |
| `backend` | SourceRuntime/ViewerLease 真相、Room/Track 对账、generation、引用计数、超时与释放 | 不依赖前端布尔状态或设备端单方上报作为 Track 最终事实 |
| `media-common` | 命令/状态 DTO 的 `runtimeId/generation/commandId` 契约 | 不容忍调用方各自定义同名字段的不同含义 |
| LiveKit/部署 | Room/Participant/Track 事实、Webhook、ICE 候选、UDP/TCP 通道与指标 | 不承载业务 viewer 计费/引用所有权 |
| 生产设备端（仓库外） | 按协议执行幂等 start/stop，上报当前 generation 和发布结果 | 其具体代码改造不在本仓库；`python-client` Demo 不作为代替实现 |

关键边界规则：只有 `backend` 基于 LiveKit 事实与 runtime 状态机才能决定是否恢复 Publisher；`control-service` 只执行已领取的命令；浏览器始终只恢复自身 viewer 连接。

## 6. 分阶段整改计划

### 第一阶段：立即止损，P0

1. 前端取消浏览器异常自动调用 Publisher restart。
2. 增加 Room generation 和 connect/restart/stop in-flight registry。
3. 补齐 Reconnected 后的 Track 枚举、重新订阅和 DOM attach。
4. Media 合并 `REQUESTING_CLIENT/ROOM_READY` 阶段的重复 restart。
5. Control 对同 session/generation 的 MQTT start 防重。
6. 修正发布超时时钟，不再被 viewer heartbeat 掩盖。
7. 增加 Duplicate Identity、重复 start 和 viewer reconnect 日志。

第一阶段目标是停止重启风暴并保证页面无需刷新即可恢复，不等待完整数据模型重构。

### 第二阶段：资源模型整改，P0/P1

1. 引入 SourceRuntime 唯一租约和数据库约束。
2. ViewerSession、对讲和录像统一引用 SourceRuntime。
3. Room、Publisher、Track 和 idle release 迁移到 SourceRuntime 生命周期。
4. 命令和状态引入 publisherGeneration。
5. 引入 LiveKit Webhook 和周期对账。
6. 向生产设备端团队交付 generation 协议契约与认证用例；设备端实现不在本仓库整改范围内。

### 第三阶段：网络与生产治理，P1/P2

1. 完成真实大屏网络 UDP 双向验证和 TCP 降级测试。
2. 建立 WebRTC stats、LiveKit、Control、Media、MQTT 和客户端统一指标。
3. 完成长稳、弱网、多用户和故障注入测试。
4. 建立灰度、回滚和生产告警规则。

## 7. 实施任务拆分

| 编号 | 模块 | 任务 | 优先级 | 前置依赖 | 整改状态 |
| --- | --- | --- | --- | --- | --- |
| FE-01 | robot-ui | 移除 viewer 异常直接 restart Publisher | P0 | 无 | 已灰度部署并通过生产故障注入：viewer 重连未触发 Publisher restart |
| FE-02 | robot-ui | Room generation 与过期回调隔离 | P0 | 无 | 已灰度部署并通过生产故障注入：重连前后 session/Room/generation/Track 不变 |
| FE-03 | robot-ui | start/connect/restart/stop Promise 在途合并 | P0 | 无 | 已灰度部署并通过生产验证：同路 20 次并发 start 仅产生一次 Media 请求 |
| FE-04 | robot-ui | Reconnected 后 Track/DOM 状态重建 | P0 | FE-02 | 已灰度部署并通过生产故障注入：三路 signal/full reconnect 后自动继续解码 |
| FE-05 | robot-ui | 分层播放状态和诊断快照 | P1 | FE-02、FE-04 | 未开始（第三阶段） |
| FE-06 | robot-ui | 将 `play()` 失败/视频刷新改为纯 viewer 本地恢复 | P0 | FE-02、FE-04 | 已灰度部署并通过生产故障注入：`play()` 拒绝和页面内刷新仅重绑 Track |
| FE-07 | robot-ui | `startCamera` 响应后校验 session/Room 一致性 | P0 | FE-03 | 整改完成：已灰度部署；三路初始出画、同路并发启动及本地切换竞态通过 |
| MS-01 | backend | 重复 restart 合并与 idempotency key | P0 | 无 | 第一阶段已灰度部署；跨实例唯一租约留待第二阶段 |
| MS-02 | backend | SourceRuntime 表、唯一键与版本控制 | P0 | 数据库变更评审 | 整改完成并已生产灰度：三路历史会话原位关联，20 次同源并发启动仍为一个 runtime/session/Room/Track |
| MS-03 | backend | 聚合引用计数和安全 Room 释放 | P0 | MS-02 | 整改完成并已生产灰度：关闭单个观看端后三路真实 viewer 均从 3 降至 2，其余观看端连续播放，未释放共享 Room 或下发 stop |
| MS-04 | backend | Publisher/Track Webhook 与对账 | P1 | LiveKit Webhook 配置 | 已部署并部分完成线上验收：Webhook、三路事实对账及观看端隔离通过；Publisher 退出故障注入待补 |
| MS-05 | backend | 发布超时改用独立命令时钟和命令 CAS | P0 | MS-01 | 第一阶段已灰度部署：`commandRequestedAt + commandId`；`generation` 留待第二阶段 |
| MS-06 | backend | ViewerLease 唯一约束、TTL 与全状态释放 | P1 | MS-02 | 未开始（第二阶段） |
| MS-07 | backend | 定时任务分布式 claim/接管 | P1 | MS-02 | 未开始（第二阶段） |
| MS-08 | backend | 消除服务启动清理与 viewer 心跳的跨行锁冲突 | P0 | 无 | 已灰度部署，重启并发观察通过，待业务验收 |
| CS-01 | control-service | MQTT start 命令防重 | P0 | MS-01 | 第一阶段已灰度部署：单实例 `commandId` 防重；分布式 generation 留待第二阶段 |
| CS-02 | control-service | 恢复调度按 SourceRuntime 执行 | P1 | MS-02 | 未开始（第二阶段） |
| CS-03 | control-service | 定时恢复/释放单执行者与命令审计 | P1 | MS-07 | 未开始（第二阶段） |
| BFF-01 | bigscreen-bff | 透传 `traceId/clientRequestId`，锁定非幂等请求不自动重试 | P1 | MS-01 | 未开始 |
| CT-01 | 跨系统契约 | 定义生产设备端 `runtimeId/generation/commandId` 命令与状态协议 | P0 | MS-02、CS-01 | 未开始（第二阶段） |
| CT-02 | 跨系统契约 | 提供设备端契约测试、乱序/重投用例和上线门禁 | P0 | CT-01 | 未开始（第二阶段） |
| OPS-01 | 部署/网络 | UDP 50000-50100 双向验证 | P1 | 现场配合 | 部分验证：本机外部 Chrome 三路均选中 UDP；机器人当前一路曾选中 UDP、当前三路为 TCP，机器人侧 UDP 稳定性待专项排查 |
| OPS-02 | 部署/监控 | LiveKit 与 WebRTC 指标和告警 | P1 | FE-05、MS-04 | 未开始（第三阶段） |
| QA-01 | 全链路 | 并发、弱网、长稳和资源释放回归 | P0 | 第一、二阶段 | 进行中（前端专项 17 个、Media 74 个、Control 122 个测试已通过；4 小时 session/viewer heartbeat 长稳、三路真实解码及短回归已通过；按当前安排暂不执行 4 小时持续解码） |

### 7.1 第一阶段实施记录（2026-09-11）

| 整改项 | 已落地行为 | 异常与边界 |
| --- | --- | --- |
| viewer 恢复 | `TrackUnsubscribed` 不再 restart Publisher；`Disconnected` 仅换取 viewer token 并重建当前 Room；`Reconnected` 重新绑定已有 Track；记录 session/Room/generation 及 `DUPLICATE_IDENTITY` 分类 | 连接期间同时收到 `Disconnected` 时，等当前 connect Promise 收口后再发起一次 viewer 重连；停止后不重连 |
| 前端并发 | 同摄像头 `start/connect/restart/stop` 分别共享在途 Promise，stop 优先 | 新 session/Room 不复用旧 Room；过期 Room 回调由 `roomGeneration` 隔离 |
| 本地播放 | 人工刷新和 `video.play()` 失败只做 Track attach/viewer 重连 | 不调用 session stop/start，不影响其他观看端 |
| Media 命令合并 | `INIT/REQUESTING_CLIENT/ROOM_READY/INTERRUPTED` 可进入同源复用查找；启动/重启先锁定 session 行，发布等待窗口内返回同一 `commandId` | 本阶段不新增表和源唯一索引；真正同时的跨 Media 实例“先查后插”新会话竞态由 MS-02 解决 |
| Control 命令防重 | 近期 2048 个 `commandId` 仅发布一次 MQTT start，发布异常时释放占位以便重试 | 当前为 JVM 内防重；多 Control 实例间的单执行者由 MS-07/CS-03 解决 |
| 发布超时 | 候选查询改用 `commandRequestedAt`，落状态前再校验当前 `commandId`、状态和时间 | viewer heartbeat 即使持续刷新 `updatedAt`，也不会掩盖发布超时；旧命令扫描不能将新命令标记失败 |
| 重启锁冲突 | 删除应用启动完成后强制关闭全部 viewer 的逻辑；历史 viewer 统一由 TTL 清理；restart 不再刷新 viewer 占用；TTL 清理改为带心跳阈值的单行条件更新且不再用一个事务覆盖全部 viewer | 服务重启期间，正在恢复的浏览器心跳不会与全量清理形成多行锁环；扫描后已经续租的 viewer 不会被过期快照误关；viewerCount 在 TTL 窗口内可能短暂包含尚未续租的历史 viewer，这是延迟释放而非资源误删 |

### 7.2 首次生产切换记录（2026-09-11）

1. Media、Control 和大屏发布包上传及 SHA-256 校验通过；仅 Media 容器进入新版本启动。
2. Media 启动后出现 MySQL 1213 死锁、锁等待超时及 Hikari 连接池耗尽，灰度立即判定失败；Control 容器未重启。
3. Media、Control 磁盘文件和大屏 `dist` 均恢复到切换前校验值，Media 重建后生产恢复；`00:53:30` 后未再观察到新死锁，线上入口返回 HTTP 200。
4. 根因是启动清理在一个事务内关闭全部历史 viewer，同时浏览器恢复 heartbeat；此外 restart 的 `session -> viewer` 锁序与 heartbeat 的 `viewer -> session` 锁序相反。
5. 再次灰度前必须补做“服务重启 + 多页面并发 heartbeat/restart”验证，并确认无死锁、锁等待和连接池排队。

### 7.3 第二次生产切换记录（2026-09-11）

1. 使用独立 `r2` 发布目录重新部署大屏、Control 和修正后的 Media，三项线上 SHA-256 与构建产物一致。
2. Control 和 Media 均正常启动；Media 启动后的稳定观察窗口内，Media/Control 错误增量均为 0。
3. MySQL `data_lock_waits` 为 0，超过 10 秒的 InnoDB 事务为 0；未再出现 1213 死锁、锁等待超时或 Hikari 连接池耗尽。
4. 大屏入口返回 HTTP 200；切换当时受现场 HTTPS 证书不受信任限制未完成登录后验收，后续已使用忽略该证书错误的隔离外部 Chrome 补做三路真实解码验证，结果见 7.4。
5. 第二次切换前版本保存在 `/home/robot-media-releases/realtime-video-fixes-20260911-r2/backup-before-cutover`。

### 7.4 四小时线上长稳记录（session/viewer 通过，真实解码待补）

- 执行时间：2026-09-11 01:50:54 至 05:50:55（Asia/Shanghai）；脚本完成 2683 轮，05:50:56 完成清理。
- 并发负载：三路 session 前 10 分钟各增加 5 个独立 viewer，随后各增加到 10 个；每 5 秒并发 heartbeat，平均 6 RPS。全程 heartbeat 失败 0，结束后查询 t1–t30 活跃 viewer 为 0。
- 服务与数据库：Media、Control、LiveKit 和 MySQL 容器全程存活；MySQL `data_lock_waits=0`，超过 10 秒的事务为 0，未再出现死锁、锁等待超时或 Hikari 连接池耗尽。
- 资源峰值采样：Media CPU 7.26%、Control CPU 19.56%、LiveKit CPU 24.85%、MySQL CPU 66.44%；结束前内存约为 795 MiB、603 MiB、119 MiB 和 1.04 GiB，未持续危险升高。
- 真实播放边界：外部 Chrome 初始已确认 `sx-songling-001` 三路出画，整页刷新后约 15 秒恢复；该浏览器 viewer 约 36 分钟后以 `CLIENT_REQUEST_LEAVE` 离开，后续负载仅保持 session/viewer heartbeat，因此不得将本记录表述为“三路持续解码 4 小时”。
- 三路解码补验：08:34 使用隔离外部 Chrome 和测试账号进入控制中心；三个可见 `<video>` 均为 `readyState=4`、分辨率 640×360、MediaStreamTrack 状态 `live`，15 秒双采样窗口内播放时间分别增长 14.992、14.945、14.456 秒。页面另有隐藏预置 `<video>` 和非视频区域“未连接”文本，不作为播放失败判据。
- viewer 信令恢复：08:42 对同一浏览器三路 Room 同时注入 LiveKit `signal-reconnect`，约 4.0 秒后全部恢复；三路 sessionId、roomName、roomGeneration 和 Track SID 前后不变。LiveKit 对三路均记录 `RR_SIGNAL_DISCONNECTED` 后原 Participant resume/ICE reconnect；Control 无视频 start/restart，Media 无 restart/新 commandId/duplicate。
- 多 viewer 隔离：08:48 使用 t6、t7 两个独立浏览器同时观看三路，t7 对三路注入 `full-reconnect` 后约 38.1 秒恢复，sessionId、roomName、roomGeneration 和 Track SID 前后不变；同时 t6 的三路在 60 秒窗口内持续解码，播放时间分别增长约 59.9、60.1、60.0 秒。Control 无视频 start/restart，LiveKit 无 Duplicate Identity 或 Publisher 替换。
- 前端并发合并：09:10 对已播放的 camera01 同时调用 20 次 `startCamera`，20 个调用返回同一 sessionId，前后 roomName、roomGeneration 和 Track SID 不变，三路继续解码；Media 对应时间窗仅收到一次 camera01 创建/复用请求，Control 无视频 start/restart，LiveKit 无 Duplicate Identity 或 Publisher 替换。
- 启停切换竞态：使用行为级自动化延迟首个 `startCamera` 响应，并在响应返回前交错执行 `stop/start`；首会话在启动收口后被停止，第二次启动才成为最终状态。另验证服务端返回不同 sessionId/roomName 时必须重连新 Room，不能沿用旧 Room。两项异常时序均通过；为避免影响同 Room 的线上观看者，未在生产环境主动销毁共享会话制造新 session。
- 本地播放恢复：09:13 对 camera01 的 `<video>.play()` 注入一次 `NotAllowedError`，确认触发两次已有 Track 重绑；随后调用页面内 `refreshVideo`。前后 sessionId、roomName、roomGeneration 和 Track SID 不变，5.25 秒后播放时间继续增长；Media、Control 和 LiveKit 对应时间窗无创建、restart、命令或 Publisher 替换日志。
- Publisher 观测：压测期间 camera02 发生两次、camera01 发生一次同 identity 连接替换，LiveKit 均在约 1 秒内恢复 active 并重新发布 Track；对应时间窗口内 Control 无匹配的 MQTT start/restart，不属于本次服务端重复启动回归，生产设备端内部重连原因属于仓库外边界。
- 结论：已关闭“4 小时 session/viewer 并发、服务重启后数据库稳定和清理”“三路初始解码”“viewer signal/full reconnect”“多 viewer 隔离”“同路并发 start”“启停切换竞态”和“本地播放恢复”子项；真实物理网络 10/30 秒断网及三路持续解码 4 小时仍需单独验收。DevTools `Network.emulateNetworkConditions` 不阻断 WebRTC UDP，不作为物理断网验收依据。
- 压测目录：`/home/robot-media-loadtests/20260911-4h-viewer`。

### 7.5 MS-02 SourceRuntime 本地验证记录（2026-09-11）

- 数据模型：新增 `media_source_runtime`，对 `source_type + source_id + device_id + channel + quality` 建数据库唯一约束，并使用 `@Version` 保存乐观版本；`media_video_session` 新增 nullable `runtime_id` 和状态索引。
- 创建顺序：先用 MySQL `INSERT ... ON DUPLICATE KEY UPDATE` 原子确保 runtime 存在，再以悲观写锁读取唯一 runtime，最后查询或创建 session；通道切换也统一为 `runtime -> session` 锁序。
- 并发验证：两个本地 Media 实例共享隔离 MySQL 8.4.9，各并发发起 25 个同源 `reuse=true` 请求；50 个响应全部成功且 sessionId、roomName 完全相同，数据库最终为 1 个 runtime、1 个 session、50 个 viewer，无长事务，两个实例均无数据库异常日志。
- 历史兼容：预置一条 `runtime_id=NULL` 的旧会话后再次创建同源 viewer，返回原 sessionId，原位补齐 runtimeId，未创建第二条 session。
- 迁移验证：一次性脚本 `deploy/database/20260911-add-media-source-runtime.sql` 已在 MySQL 8.4.9 实际执行通过；生产当前同版本、`REPEATABLE-READ`，会话表约 1.5 MiB，脚本使用 `LOCK=NONE` 增加 nullable 列和索引。
- 回归：Media 全量 51 项测试通过。当前未把 Room 删除、viewer/对讲/录像聚合引用和 Publisher generation 迁入 runtime，分别留给 MS-03、MS-05、MS-06 与 CT-01；本项已按下述边界完成生产灰度。
- 上线边界：先备份 `media_video_session`，再灰度单个 Media 实例并核对新表唯一约束、历史会话原位关联和数据库锁等待；应用回滚时保留新表及 nullable 列，不删除已生成的 runtime 数据。

### 7.6 MS-02 SourceRuntime 生产灰度记录（2026-09-11）

- 灰度范围：只迁移 `robot_media` 数据模型并切换单个 `robot-mediaserver-media-service` 的 `boot/lib`；保留线上 `config/bin`，Control、UI、LiveKit 均未重启。
- 产物校验：线上发布包 SHA-256 为 `4503f85f80dad864b69baa2b5e53bebf576fa83bd835dacf306a0eb9f6578eef`，迁移 SQL SHA-256 为 `832db5616ec0ea55794422ab7c285f2fcbbbb3a811b679c6d2302ee9a7454402`，与本地构建产物一致。
- 数据迁移：MySQL 8.4.9 成功建立 `media_source_runtime`、五字段源唯一键、nullable `media_video_session.runtime_id` 和 `runtime_id + status` 索引；执行后无长事务、无 `data_lock_waits`。
- 历史兼容：`sx-songling-001` 的 `camera01`、`camera02`、`songling-camera` 三条既有 STREAMING 会话均原位复用并补齐各自 runtimeId，没有新建第二个 session；三路各有且仅有一条 SourceRuntime，全库源唯一重复查询为空。
- 大屏验证：隔离外部 Chrome 登录唯一三路页面并进入控制中心，三个可见 `<video>` 均为 `readyState=4`、640×360、MediaStreamTrack `live`；15 秒内播放时间分别推进 14.946、15.011、14.985 秒。
- 并发与持续播放：对 camera01 同时发起 20 次 `startCamera`，全部返回原 sessionId；roomName、roomGeneration、Track SID 均不变。随后连续观察 60 秒，三路分别继续推进约 60 秒，无断流。
- 服务稳定性：Media 在 17.9 秒内启动，观察 10 分钟无容器重启；启动后无应用异常日志、无超过 10 秒的 InnoDB 事务、无数据库锁等待。
- 回滚点：`/home/robot-media-releases/source-runtime-20260911-1058/backup-before-cutover` 保存切换前完整 Media 应用和 `media_video_session/media_session_viewer` 数据；若仅回滚应用，保留新增表、nullable 列和已生成 runtime 数据。
- 范围边界：本次仅关闭“同源创建唯一性和历史会话关联”。共享 Room 安全释放、viewer/对讲/录像聚合引用、Publisher generation、跨实例定时任务 claim 仍分别由 MS-03、MS-05、MS-06、MS-07、CS-02/03、CT-01 处理。

### 7.7 MS-03 聚合引用与安全 Room 释放本地验证记录（2026-09-11）

- 锁与聚合边界：viewer 心跳/停止、对讲开始/心跳/停止和录像开始均先锁 SourceRuntime、再锁 session；空闲释放使用同一锁序，随后按已有 `runtime_id` 索引锁定同源会话，并按五字段源键补查 `runtime_id=NULL` 的历史会话。
- 释放判定：使用真实 active viewer 行数，不信任缓存 `viewerCount`；任一同源 session 仍有 viewer、`STARTING/ACTIVE` 对讲、进行中的 LiveKit Egress 录像、未超时启动命令或未到期空闲期限时，不删除共享 Room、不返回设备 stop 载荷。无引用且全部到期后，关闭同源残留 session、删除 Room，并只返回一次 stop 载荷；重复释放返回空结果。
- 生命周期边界：心跳和对讲开始不得复活 `CLOSED/STOPPING` 会话；stop 在源锁内强制刷新数据库最新 session，不能用请求级 EntityManager 的旧快照把 `CLOSED` 覆盖回 `IDLE_WAIT`。旧 session 无独立占用时可以只关闭自身记录，但不能影响同源其他观看者。Publisher generation 和 LiveKit Track 事实仍分别留给 CT-01/MS-04，本项不把 Track 存在本身当作永久占用。
- 自动化回归：新增共享 viewer、对讲、录像、启动在途、较新空闲期限、无引用最终释放、历史 `runtime_id=NULL` 和 stop/释放竞争八类测试；Media 全量 60 项测试通过。
- 真实数据库竞争：使用隔离 MySQL 8.4.9 对 30 个不同 runtime 同时发起 heartbeat/release。初版按 `room_name` 悲观查询因缺少索引触发 MySQL 1213，已改为 `runtime_id` 索引查询加精确历史源查询；最终重跑中 30 个 release 均为 HTTP 200，heartbeat 为 14 个 HTTP 200、16 个 HTTP 409（释放先获得源锁），`closed_with_active_viewer=0`、viewer 缓存偏差为 0、`data_lock_waits=0`，应用无死锁日志。
- stop/释放竞争：首次 30 组并发暴露外层 stop 事务持有连接后同步申请录像 `REQUIRES_NEW` 连接，连接池 10 个连接全部占用并互等；将录像收口移到源锁事务之前后，30 个 stop 和 30 个 release 均为 HTTP 200。随后通过拿锁后强制刷新 session 消除旧快照覆盖，最终 30 个 release stop 载荷对应 30 个 `CLOSED`，`released_but_revived=0`，无连接池等待、长事务或数据库锁等待。
- 同 Room 功能验证：两个 session 指向同一 runtime 时，给其中一个建立真实 viewer 后释放旧 session，旧记录关闭但活跃 session 和 viewer 保留，返回空载荷；viewer 停止且空闲到期后首次释放返回一个 stop 载荷，再次释放为空。
- 上线边界：本地验证覆盖对讲、录像和无 viewer 最终释放，生产灰度覆盖独立观看端离开时的共享 Room 保护，结果见 7.8。`deleteRoom`、数据库提交和 Control/MQTT stop 仍不是跨服务原子事务，进程崩溃窗口下的持久 release generation 与命令审计留给 MS-07/CS-03/CT-01。

### 7.8 MS-03 聚合引用与安全 Room 释放生产灰度记录（2026-09-11）

- 灰度范围：仅切换单个 `robot-mediaserver-media-service` 的 `boot/lib`，保留线上 `config/bin`；Control、UI、LiveKit 和 MySQL 均未重启。发布包 SHA-256 为 `43acb51a90281bb2c20fbe90aeef8d415f9ad3b7fe1ec0ed6fd49ad68e86d932`，线上启动 Jar SHA-256 为 `f898c135a818236ed8ceefb8449d047662336056b07f817bcbf0d2737e723acf`。
- 切换前基线：三个 SourceRuntime 均存在；其中 `songling-camera` 为 `STREAMING`，`camera01/camera02` 的 Room 已在切换前消失。MySQL 无 `data_lock_waits` 和超过 10 秒的事务，避免将既有 Room 状态归因于本次版本。
- 启动与恢复：Media 在 16.754 秒内启动完成，容器保持 `running` 且重启次数为 0。进入 `sx-songling-001` 控制中心后三路均恢复为 `STREAMING`，三个可见 `<video>` 均为 `readyState=4`、640×360。
- 多 viewer 隔离：两个独立外部 Chrome 同时打开三路画面，数据库三路真实 active viewer 均为 3；关闭其中一个浏览器后均准确下降为 2，缓存 `viewerCount` 与真实 active viewer 行数一致。剩余页面三路继续播放，60 秒观察窗口内持续推进，未出现黑屏或暂停。
- 共享资源保护：关闭测试观看端时 LiveKit 三路均记录正常 `CLIENT_REQUEST_LEAVE`；Media 无 Room 删除、释放或停止推流日志，Control 无对应视频 stop，剩余观看端和三个 Publisher 均未受影响。
- 网络与稳定性：本次外部 Chrome 的 LiveKit selected candidate 为 `udp4 211.137.109.150:50055`，`connectionType=udp`。灰度观察期内 Media 无应用异常、1213、锁等待或 Hikari 连接池耗尽；MySQL `data_lock_waits=0`、超过 10 秒事务为 0。
- 回滚点：`/home/robot-media-releases/ms03-room-release-20260911-1238/backup-before-cutover` 保存切换前完整 Media 应用和 `media_source_runtime/media_video_session/media_session_viewer` 数据，`runtime-before-cutover` 另保留原始 `boot/lib`。本次未触发回滚。
- 范围边界：生产灰度已关闭“一个观看端离开导致同源共享 Room 被释放”的主路径。对讲、录像和无 viewer 最终释放已由单测及隔离 MySQL 覆盖，未在生产主动中断真实推流制造场景；`deleteRoom`、数据库提交和 Control/MQTT stop 的跨服务原子性仍由 MS-07、CS-03 和 CT-01 后续处理。

### 7.9 MS-04 Publisher/Track Webhook 与对账本地验证记录（2026-09-11）

- 事实边界：机器人与固定摄像头的进程状态只推进到 `ROOM_READY`；Media 只接受预期
  Publisher Identity 下的真实视频 Track，拒绝同 Room 其他 Participant 的 Track。
- 事件与补偿：新增签名 Webhook 入口，支持 Publisher/Track/Room 关键事件；事件只触发 Room API
  当前事实查询，5 秒周期扫描补偿丢失、重复和乱序事件。
- 状态收敛：Track 出现后写入 runtime 实际 Participant/Track 字段并推进关联会话；Track 消失时仅处理
  原 `STREAMING` 会话，按 viewer、对讲和录像占用分别进入 `INTERRUPTED` 或 `IDLE_WAIT`。
- 写入控制：稳定的 Publisher/Track 事实不重复保存 runtime/session，也不重复发布 Track 或
  `video.session.streaming`，避免三路视频长期运行产生固定频率写放大。
- 自动化回归：Media 全量 74 项测试通过，覆盖签名正文防篡改、忽略无关事件、精确 Publisher 匹配、
  Track 出现/消失状态收敛、空闲等待核对、录像占用恢复、迟到设备状态隔离和稳定事实零重复写入。
- 迁移验证：一次性脚本已在隔离 MySQL 8.4.9 实际执行通过，五个 nullable 事实列和
  `room_name` 唯一索引均生效，重复 Room 写入按预期返回 1062。
- 生产只读准入：`192.168.124.23` 当前 `room_name` 重复数为 0，五个事实列和唯一索引均尚未创建；
  Media 与 LiveKit 同在 `robot-mediaserver` bridge 网络、容器状态 `running` 且重启次数为 0，现有
  LiveKit 配置尚无 Webhook，符合“先迁移、再切 Media、最后启用 Webhook”的灰度顺序。
- 首次灰度回滚：生产迁移成功后切换 Media，精确对账发现实际 JWT Publisher Identity 为
  `robot:robot:{robotId}:{deviceId}:{deviceId}`，而命令载荷声明为 `robot:{robotId}:{deviceId}`；三路因
  身份不匹配进入发布超时，已立即回滚应用并恢复三路 `STREAMING`，数据库兼容列保留。根因是生成
  Token 时把已完整构造的 identity 再传给三参数重载重复拼接；已删除该重载并统一使用完整 identity
  签发 Token，重新灰度时需受控重启三路 Publisher，使现存旧身份切换为修正后的身份。
- 二次灰度边界：重灰度时 Media 启动窗口内旧 viewer 尚未续租，旧身份 Track 消失后会话先进入
  `IDLE_WAIT`；随后 viewer 心跳只恢复计数、不会触发 Track 恢复。已补齐 `IDLE_WAIT` 心跳分支：
  Track 存在恢复 `STREAMING`，Track 缺失进入 `INTERRUPTED` 并交由既有恢复调度处理。
- 上线前置：备份 `media_source_runtime`，检查 `room_name` 无重复，执行
  `deploy/database/20260911-add-livekit-media-facts.sql`，渲染 `LIVEKIT_WEBHOOK_URL` 后重建 LiveKit；
  应用回滚时保留新增 nullable 事实列和唯一索引。
- 范围边界：生产迁移、应用切换和 Webhook 投递验证已完成；尚未执行踢出 Publisher 故障注入。
  generation 与多实例单恢复分别留给 CT-01 和 MS-07/CS-03。

### 7.9.1 MS-04 当前整合版本线上短验收（2026-09-11）

- 部署版本：分支 `codex/realtime-video-fixes` 的 `c4f7e813` 已更新到 `192.168.124.23`，
  Media、Control、大屏和 LiveKit Webhook 均已切换；发布目录为
  `/home/robot-media-releases/integration-c4f7e813-20260911-1700`，切换前备份保留在其
  `backup-before-cutover` 子目录。
- 三路事实：`songling-camera`、`camera01`、`camera02` 均实际出画并保持 `STREAMING`；每路分别绑定
  唯一 session、SourceRuntime、Room、Publisher Participant SID 和视频 Track SID。整页刷新、增加或移除
  观看端以及单独移除再添加 `camera01` 后，上述 Publisher/Track 事实均未替换。
- 页面恢复：整页刷新后重新进入控制中心并选择双光云台，三路均自动恢复；单独从布局移除
  `camera01` 时另外两路持续播放，再次添加后该路在不重启 Publisher 的情况下恢复出画。
- 多观看端隔离：第二个浏览器加入后，三路 `viewerCount` 均精确增加 1；关闭第二个页面后三路均精确
  减少 1，首个页面三路持续播放。LiveKit 收到对应 `participant_joined/participant_left`，未观察到重复
  MQTT start、`DUPLICATE_IDENTITY`、新 `commandId` 或 Publisher 替换。
- ICE 事实：当前外部 Mac Chrome 的三路观看连接均选中 UDP，LiveKit 端口分别落在
  `50000-50100/udp`；同时间段部分 Windows Chrome 观看连接按各自网络条件降级为 TCP。机器人发布端
  当前三路均选中 TCP，历史日志中 `songling-camera` 曾成功选中 UDP，因此两段链路必须独立记录，
  不能用前端 UDP 结果代替机器人发布端验收。
- 未执行边界：按当前安排不进行 4 小时持续解码长稳；本轮未主动踢出生产 Publisher，也未对用户机器
  或生产网络执行 10/30 秒物理断网。以上项目不得标记为通过，分别保留在 MS-04、OPS-01 和 QA-01。

### 7.10 大屏线上验证路径（部署后）

验证入口：[https://211.137.109.150:4443/bi/patrol/monitor](https://211.137.109.150:4443/bi/patrol/monitor)。以 `sx-songling-001` 为单机灰度对象，按以下顺序验收：

1. 打开三路画面，记录 sessionId、roomName、commandId 和初始 ICE protocol；确认每路只有一个 Publisher Participant/Track。
2. 同一浏览器快速连续点击打开/刷新；确认不产生多个 session，同一发布等待窗口中 commandId 不变，Control 最多发一条 MQTT start。
3. 同时打开两个浏览器窗口，仅让其中一个断网 10 秒后恢复；该窗口应自动出画，另一窗口连续播放，无 session restart/MQTT start。
4. 对单个窗口执行 30 秒断网、网络切换和页面内“刷新视频”；均应在不刷新整页的情况下重建 viewer Room，Publisher Participant/Track 不替换。
5. 在非生产流量时段验证“设备不发布 Track、页面持续心跳”；会话应在配置的发布超时后进入 FAILED，而不是被 heartbeat 无限延后。
6. 每个用例同时核对浏览器 Console/Network、Media、Control、MQTT 和 LiveKit 日志；若出现 `DUPLICATE_IDENTITY`、60 秒内重复 start、其他 viewer 断流或需要整页刷新，则灰度不通过。

## 8. 必须补充的测试矩阵

| 场景 | 操作 | 预期结果 |
| --- | --- | --- |
| 单 viewer 短断网 | 阻断网络 10 秒后恢复 | 自动出画，不发 MQTT start |
| 单 viewer 长断网 | 阻断网络 30 秒后恢复 | 重建 viewer Room，其他 viewer 不受影响 |
| 多 viewer 隔离 | 三个浏览器观看，同步关闭一个 | 其余两个连续播放，Publisher 不重启 |
| Room 事件竞态 | 同时触发 unsubscribe/disconnect/reconnect | 旧事件不覆盖新 Room，最多一个恢复 Promise |
| 同源并发启动 | 50 个并发 start | 一个 runtime、一个 Room、一个有效 Publisher |
| restart 并发 | 20 个相同幂等 key 的 restart | 一个 commandId、一条 MQTT 命令 |
| MQTT 重投 | 向生产设备端重复投递相同 start | 只上报同一 runtime/generation 结果，LiveKit Participant/Track 不替换 |
| 发布超时 | viewer 继续心跳，设备端不发布 Track | 按 publish deadline 超时，不被 heartbeat 延后 |
| viewer 并发续租 | 同一 viewer 50 个并发 heartbeat | 只有一条 active lease，计数不增长 |
| 多实例扫描 | 2 个 Control + 2 个 Media 同时扫描 | 同一 runtime 只有一个执行者产生副作用 |
| 迟到命令 | 新 generation 生效后投递旧 start/stop/status | 当前 Publisher 和状态不变化 |
| 共享 Room 释放 | 人工构造两个 session 引用同一 runtime | 仍有任一占用时不得删除 Room |
| 对讲占用 | 无视频 viewer、对讲 active | Room 和音频 Track 保留 |
| 录像占用 | viewer 离开、录像仍在收口 | 按正式录像规则完成，不误删源 Room |
| Publisher 真实退出 | 杀死机器人 Publisher | Media 确认 Track 消失，只启动一次恢复 |
| UDP 正常 | 现场开放 UDP 全链路 | selected candidate pair 为 UDP |
| TCP 降级 | 明确阻断 UDP | 自动走 TCP，抖动恢复后不永久未连接 |
| 三路弱网 | 5% 丢包、300 ms RTT、带宽受限 | 主画面可用，非主画面降载，不重启 Publisher |
| 页面布局切换 | 视频墙、弹窗、全屏反复切换 | Track 幂等 attach，无额外 start |
| 长稳播放 | 三路视频连续播放 8 小时 | 无永久黑屏、无重复身份、无重复 start |
| 生产耐久 | 典型生产网络连续运行 24 小时 | 无需人工刷新或 restart，资源无持续增长 |

## 9. 上线门禁与验收指标

以下条件全部满足后，实时视频整改版本才能结束灰度：

1. P0 问题全部关闭，并有自动化或可重复的故障注入证据。
2. 单浏览器网络异常不会产生机器人 start/restart。
3. 同源并发启动只能得到一个有效 SourceRuntime 和 Publisher。
4. 生产日志中连续 24 小时无 `DUPLICATE_IDENTITY`、无 60 秒内重复 start。
5. 三路视频 8 小时长稳测试无永久黑屏；短暂中断后 10 秒内恢复。
6. 页面不刷新完成网络切换、LiveKit 重连和 Track 重新绑定。
7. Room 删除时 viewer、对讲、录像和启动操作引用数全部为 0。
8. UDP 可用网络优先选择 UDP；TCP 降级场景有独立通过记录。
9. 所有失败均能从诊断数据区分：信令、ICE、Publisher、Track、订阅、DOM 绑定或解码问题。
10. Token、RTSP 地址、密码和完整 SDP 不进入普通生产日志。

## 10. 灰度与回滚要求

- 第一阶段先灰度单个测试机器人和单个大屏；若涉及生产设备端协议升级，由对应外部团队独立灰度。
- 前端与后端协议变更必须向后兼容一个灰度周期；generation 字段未升级的客户端不得进入多 Publisher 争抢状态。
- SourceRuntime 数据模型迁移前备份相关表，并提供只回滚应用、不删除新表数据的回滚方案。
- 每次灰度记录版本、机器人 ID、浏览器版本、网络类型、最终 ICE 协议和测试 sessionId。
- 发现重复 start、Duplicate Identity 或 Room 误删除时立即停止扩大灰度。

## 11. 当前限制与待确认项

1. 本文线上证据来自 `192.168.124.23` 的 LiveKit、Control 和 Media 容器日志；浏览器 Console 与 `webrtc-internals` 尚未同步采集。
2. 当前无法仅凭服务端候选列表判断大屏侧 UDP 被哪一层阻断，需要客户端和服务端同步抓包。
3. 服务器活动会话接口要求可信身份头，本次排查没有绕过鉴权读取生产会话数据。
4. 生产设备端媒体客户端不在本仓库；本文不对其实现做结论，只将协议契约、连通性和端到端验收作为外部边界。
5. 当前工作区存在其他未提交功能改动，实施整改时必须拆分提交，避免将无关变更带入生产补丁。

## 12. 代码审阅范围与闭环定位

本文结论不只基于单个日志或单个方法，已按“前端入口 -> BFF -> Control -> Media -> MQTT 协议 -> LiveKit -> 前端播放”检查主链路及定时任务、释放和部署配置。关键代码定位如下：

| 链路环节 | 关键代码 | 确认的事实 |
| --- | --- | --- |
| 前端 API | `robot-ui/src/api/media.js:17-80` | `clientRequestId` 字段可传，但实际 start 调用未生成；start/heartbeat/stop/restart 均为 POST |
| 前端会话 | `robot-ui/src/store/modules/websocket-robot.js` 的 `startCamera/connectLiveKit/recoverCameraPlayback/restartCamera/stopCamera` | 已收缩为 viewer 恢复与 Publisher 人工重启两条路径，并加入在途合并和 Room generation 隔离 |
| 前端本地播放 | `robot-ui/src/views/bi/patrol/monitor/first/LeftVideo.vue` 的 `resumeVideo/refreshVideo` | 已取消 `play()` 失败调 start 和人工刷新 stop+start，仅恢复当前 viewer |
| 前端展示 | `robot-ui/src/views/bi/patrol/monitor/first/VideoBox.vue:93-100` | 故障主要被折叠为“正在连接/未连接” |
| BFF | `bigscreen-bff/src/main/java/com/robot/bigscreen/api/BigscreenProxyController.java:33-42`<br>`bigscreen-bff/src/main/java/com/robot/bigscreen/client/CenterProxyClient.java:71-89` | 当前是通用转发边界，应继续保持不承载媒体业务 |
| Control 启动/重启 | `control-service/src/main/java/com/robot/control/service/ControlVideoCommandService.java` 的 `sendStart` | 已增加单实例近期 `commandId` 防重；跨实例 claim 仍待第二阶段 |
| Control 定时任务 | `control-service/src/main/java/com/robot/control/scheduler/ControlVideoSessionScheduler.java:47-106` | 每个 JVM 都扫描恢复/释放，无 claim |
| MQTT 状态 | `media-common/src/main/java/com/robot/media/common/video/VideoStatusMessage.java:11-104`<br>`control-service/src/main/java/com/robot/control/messaging/RobotMediaStatusSubscriber.java:176-186` | status DTO 无 `commandId/generation`，Control 解析后直接转 Media |
| Media 创建/复用 | `backend/src/main/java/com/robot/mediaserver/video/service/VideoSessionService.java` 的 `create`<br>`backend/src/main/java/com/robot/mediaserver/video/model/VideoSourceRuntime.java` | 已引入源唯一约束和 `runtime -> session` 锁序；本地跨实例并发及生产三路历史关联、20 次同源并发复用均通过 |
| Media Track 事实 | `backend/src/main/java/com/robot/mediaserver/video/service/VideoSessionService.java`<br>`backend/src/main/java/com/robot/mediaserver/livekit/LiveKitWebhookService.java` | 已改为 Webhook 触发和周期 Room API 对账；设备状态不再直接推进 `STREAMING/INTERRUPTED` |
| Media 命令 | `backend/src/main/java/com/robot/mediaserver/video/service/VideoSessionService.java` 的 `requestClientStart` | 已通过 session 行锁与发布等待窗口复用同一 commandId |
| Media 释放 | `backend/src/main/java/com/robot/mediaserver/video/service/VideoSessionService.java:914-938` | 只检查当前 session 即 delete Room |
| Media 超时 | `backend/src/main/java/com/robot/mediaserver/video/scheduler/VideoSessionTimeoutScheduler.java`<br>`backend/src/main/java/com/robot/mediaserver/video/service/VideoSessionService.java` 的 `markTimeout` | 已改用 `commandRequestedAt` 选候选并以 session 行锁 + `commandId` 二次校验，不再受 viewer heartbeat 影响 |
| Viewer 租约 | `backend/src/main/java/com/robot/mediaserver/video/service/VideoSessionService.java:1043-1080,1173-1207`<br>`backend/src/main/java/com/robot/mediaserver/video/model/MediaSessionViewer.java:10-43` | 先查后插无唯一键，过期后只对 STREAMING 进入 idle |
| 服务启动 | `backend/src/main/java/com/robot/mediaserver/video/scheduler/VideoSessionTimeoutScheduler.java` 的 `sweepViewers` | 已取消启动时关闭全部 viewer，改由心跳 TTL 自然收敛 |
| LiveKit 对账 | `backend/src/main/java/com/robot/mediaserver/livekit/LiveKitRoomService.java` | 只解析预期 Publisher Identity 下的视频 Track，preferred SID 不存在时也不跨 Participant 兜底 |
| LiveKit 部署 | `deploy/docker/config/livekit/livekit.yaml`<br>`deploy/docker/docker-compose.yml` | 配置 TCP 7881、UDP 50000-50100 和内部签名 Webhook；映射存在不等于现场 UDP 可达 |

代码证据同时记录已整改现状和剩余风险；涉及生产实例数、现场 UDP 阻断点、设备端内部行为的结论，必须继续以部署现状、双端抓包和外部客户端契约测试为准。

## 13. 相关文档

- [机器人实时视频设计说明书](机器人实时视频设计说明书.md)
- [实时视频普通播放时序图](实时视频普通播放时序图.md)
- [实时视频接口与协议文档](../../03-接口与协议/实时视频/实时视频接口与协议文档.md)
- [实时视频模块测试方案](../../04-测试与验收/测试方案/实时视频模块测试方案.md)
- [实时视频模块验收用例](../../04-测试与验收/验收用例/实时视频模块验收用例.md)
