# 普通控制与语音对讲回归验收记录（2026-09-22）

## 1. 验收结论

- 验收结论：**通过**。
- 本次重新构建并部署了 `robot-ui` 最新代码，线上页面加载的文件与本地构建产物哈希一致。
- 普通扬声器音量、静音、38mm 发射器连接判定、实时监控普通对讲、地图远程控制普通对讲均完成回归。
- 本轮未发现新的功能缺陷或未闭环问题。
- 本次范围不包含多合一设备业务；未修改 CSS、布局和视觉样式。

## 2. 执行信息

| 项目 | 内容 |
| --- | --- |
| 执行日期 | 2026-09-22 |
| 执行环境 | `192.168.124.23`，公网入口 `https://211.137.109.150:4443` |
| 前端模块 | `robot-ui` |
| 验收设备 | `sx-songling-001`（轮式机器人 R1） |
| 验收浏览器 | Chrome |
| 相关整改提交 | `58730fe9`、`58ae9e38`、`9c92f2d3` |
| 部署目录 | `/home/mounts/media/nginx/html/dist` |
| 部署备份 | `/home/mounts/media/nginx/html/dist.bak-202609222230-full-regression` |

## 3. 构建与部署记录

### 3.1 自动化测试与构建

在 `robot-ui` 执行：

```bash
node --test test/*.test.mjs
npm run build:prod
```

实际结果：

- 全量前端测试：220 项通过，0 项失败。
- 生产构建：通过；仅存在项目原有的包体积提示，没有构建错误。
- 音量状态专项：11 项通过，0 项失败。
- 对讲生命周期专项：35 项通过，0 项失败。
- 远程控制及 38mm 安全判定专项：18 项通过，0 项失败。

### 3.2 部署一致性

| 对象 | 实际值 |
| --- | --- |
| 构建入口文件 | `dist/index.html` |
| 入口文件 SHA-256 | `dd6c78bdd1f67df98850c17fdf22034665925b4541f53176f60fe4ca7044a3f4` |
| 主应用文件 | `static/js/app.c0bdcfd0.js` |
| 部署包 | `/tmp/robot-ui-regression-20260922.tar.gz` |
| 部署包 SHA-256 | `4ffb15d51abe9afc4204c547899a42512b53ec881be154f05c6ddb77a1f6da22` |

部署后分别访问内网和公网入口，两端 `index.html` 的 SHA-256 均与本地构建一致，主应用文件均返回 HTTP 200。

## 4. 线上功能回归

### 4.1 普通扬声器音量与静音

| 用例 | 操作与实际结果 | 结果 |
| --- | --- | --- |
| 减音量联动 | 初始值 85，点击减号后滑块和值立即变为 80，设备再次上报后仍为 80 | 通过 |
| 加音量联动 | 点击加号后滑块和值立即由 80 变为 85 | 通过 |
| 拖动滑块 | 从 85 拖到约 61，等待超过设备 3～5 秒上报周期后仍保持 61，未出现回退 | 通过 |
| 命令路由 | 三次音量命令依次产生 `seq=1/2/3`，目标均为 `deviceId=speaker_main`、`deviceType=SPEAKER` | 通过 |
| 静音显示 | 音量 61 时静音，滑块保留在 61，显示“已静音，音量 61”，滑块和加减键禁用 | 通过 |
| 取消静音 | 取消静音后恢复显示 61，滑块和加减键恢复可用 | 通过 |
| 静音命令 | 静音、取消静音分别产生 `seq=4/5`，目标均为 `speaker_main` | 通过 |

结论：交互期间由本地值保证操作连续性，交互结束后由 `devices.status.volume/muted` 最终校准；物理设备 `speaker_main` 仅承担设备关联和命令路由。

### 4.2 38mm 发射器连接状态

| 用例 | 实际结果 | 结果 |
| --- | --- | --- |
| 未明确连接 | 当前设备未上报明确的 `connected=true`，页面显示“未连接” | 通过 |
| 安全联锁 | 未连接时安全开关关闭，六个发射按钮全部禁用 | 通过 |
| 判定一致性 | 页面展示、按钮禁用和执行入口共用同一完整安全判定 | 自动化通过 |

当前环境没有可安全验证的 `connected=true` 发射器，因此未执行真实发射和线上“已连接”正向用例。自动化测试已覆盖：无连接状态、明确断开、连接但设备离线、连接且在线，以及发射必须同时满足连接、安全开关和装载状态。

### 4.3 实时监控页面普通对讲

| 用例 | 实际结果 | 结果 |
| --- | --- | --- |
| 首次开启 | 点击“点击通话”后浏览器开始采集麦克风，页面变为“正在通话 点击结束” | 通过 |
| 主动挂断 | 点击结束后浏览器麦克风录音标识消失，页面恢复“点击通话” | 通过 |
| 页面退出清理 | 通话中从实时监控切换到全景地图，麦克风录音标识立即消失 | 通过 |

### 4.4 地图远程控制普通对讲

按“机器人信息面板 → 远程控制面板 → 开启语音”的原问题路径执行：

| 用例 | 实际结果 | 结果 |
| --- | --- | --- |
| 关闭机器人信息面板 | 通话保持，远程控制面板仍显示“正在通话” | 通过 |
| 信息面板关闭后挂断 | 麦克风释放，按钮恢复“点击通话” | 通过 |
| 信息面板关闭后再次开启 | 可重新开启通话，未出现按钮点不动 | 通过 |
| 关闭远程控制面板 | 通话中关闭远程控制面板，麦克风立即释放 | 通过 |

### 4.5 服务端与 LiveKit 生命周期证据

回归时间窗 `22:41:00～22:46:00` 内：

- Control Service 记录 4 次 `intercom/start` 和 4 次 `intercom/stop`，启停成对。
- Media Service 记录 4 次浏览器麦克风 `track_published` 和 4 次相同 Track 的 `track_unpublished`，发布与取消发布成对。
- 对应机器人对讲参与者均出现 `track_unpublished` 和 `participant_left`。
- Control Service、Media Service、Bigscreen BFF、Nginx 在本轮完整回归时间窗内未检出 `ERROR`、`Exception` 或 5xx 日志。

## 5. 入口与影响边界复核

- 实时监控远程控制和地图远程控制均复用普通对讲生命周期管理。
- 地图左侧任务列表的视频按钮只在任务存在活动流程实例时显示；当前全部为等待任务，为避免启动真实任务和控制实体机器人，未强行制造活动任务。
- 任务视频弹窗本身是视频查看入口，不直接提供对讲；进入“控制中心”后才进入普通对讲链路，因此不属于遗漏的独立对讲入口。
- 多合一设备的音量、静音和通话流程不在本次变更范围，代码未改动其组件和业务路径。
- 本次整改及本轮记录均未修改 CSS 文件、布局尺寸或样式选择器；线上实时监控和地图页面布局显示正常。

## 6. 未执行的破坏性或环境受限用例

以下场景未在生产页面主动制造，不能记为线上实测通过：

- 浏览器麦克风权限拒绝或权限在通话中被撤销。
- 网络中断、WebSocket 断线和服务端挂断超时。
- 多摄像头并发抢占和跨设备同时发起对讲。
- 38mm 发射器明确连接后的真实发射。
- 执行真实任务后从左侧任务列表打开任务视频。

上述普通对讲异常路径已由 35 项生命周期专项测试覆盖，包括权限移除、设备离线、最终断线、重复点击、在途取消、迟到响应、跨摄像头互斥、挂断确认失败、Room 断开超时和页面退出并发清理。生产环境未为测试故意注入故障或触发真实发射、真实任务。

## 7. 缺陷记录

本轮回归未发现新增缺陷。环境受限项已在第 6 节登记，不作为缺陷关闭，也未虚构为线上已通过。
