# 内部部署脚本

本目录由公开入口脚本调用，不面向实施人员直接使用。

- 安装与配置统一执行根目录 `install.sh`。
- 卸载统一执行根目录 `uninstall.sh`。
- 打包统一执行根目录 `package.sh`。
- 开发增量发布统一执行根目录 `update-services.sh`。

调整本目录脚本时，需要同时验证源码目录和 `package.sh` 生成的安装包目录结构。

`validate-env.sh` 负责校验已启用能力的条件配置。公开安装和增量更新入口都必须在
覆盖程序或启动容器前调用它；新增带开关且依赖多项配置的能力时，应同步补充对应校验。
